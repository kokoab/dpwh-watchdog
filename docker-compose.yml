services:
  backend:
    build: ./backend
    container_name: backend_watchdog
    ports:
      - "8000:8000"
    # env_file:
    #   - .env
    volumes:
      - ./backend:/app
    tty: true
    # environment:
    #   - name=value
  